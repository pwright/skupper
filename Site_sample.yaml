apiVersion: skupper.io/v2alpha1
kind: Site
spec:
  defaultIssuer: string
  edge: true
  ha: true
  linkAccess: string
  serviceAccount: string
  settings: {}
status:
  conditions:
  - lastTransitionTime: 2024-10-11T12:48:44Z
    message: string
    observedGeneration: 0
    reason: tkZ # ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
    status: "True" # "True", "False", "Unknown"
    type: 8rz10lr9o.y.cghkk9vz.f.sye.kq.z-i/0 # ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][- A-Za-z0-9_.]*)?[A-Za-z0-9])$
  defaultIssuer: string
  endpoints:
  - group: string
    host: string
    name: string
    port: string
  message: string
  network:
  - id: string
    links:
    - name: string
      operational: true
      remoteSiteId: string
      remoteSiteName: string
    name: string
    namespace: string
    platform: string
    services:
    - connectors: [] # minItems 0 of type string
      listeners: [] # minItems 0 of type string
      routingKey: string
    version: string
  sitesInNetwork: 1
  status: string
